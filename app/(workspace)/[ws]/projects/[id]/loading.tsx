import { DetailSkeleton } from "@/components/primitives/page-skeleton";

// The full page route. The floating panel has its own inside the @modal slot.
export default function ProjectLoading() {
  return <DetailSkeleton />;
}
